[project]
name = "ptrade-quant-lab"
version = "0.1.0"
description = "Local A-share daily backtest and PTrade code generator"
requires-python = ">=3.11"
dependencies = [
  "pandas>=2.2.0",
  "pyyaml>=6.0.0",
  "tabulate>=0.9.0",
]

[tool.uv]
package = false
